# Next.js
.next/
out/

# Dependencies
node_modules/

# Environment variables (never commit these)
.env.local
.env.*.local

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/settings.json
*.swp
*.swo

# Legacy backup (local only — filenames too long for Windows Git)
_legacy/

# Payload CMS + Auth SQLite databases (local data, never commit)
data/payload.db
data/payload.db-shm
data/payload.db-wal
data/payload-build.db
data/payload-build.db-shm
data/payload-build.db-wal

# Sitemaps (generated at build time)
public/sitemap*.xml
public/robots.txt

# Uploaded media (managed by Payload, preserved on VPS separately)
public/media/

# SSH keys — NEVER commit private keys
*.pem
id_rsa
id_rsa.*
id_ed25519
id_ed25519.*
sprecher-east
sprecher-east.pub

# Claude Code — local settings (project settings.json is OK to commit)
.claude/settings.local.json
CLAUDE.local.md

# Playwright MCP
.playwright-mcp/

# Local screenshots
homepage-local.png
