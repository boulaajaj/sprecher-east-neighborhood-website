# AI-mise harness audit — September 6, 2026

## Scope and evidence

Request: check out the website here, use AI-mise to put the repository harness in
order, and report Graphify and Code Cohesion status with result links.

Baseline: `ae70d28` from `origin/main`. Audit used the locally installed AI-mise
skill at version 0.1.0, its orient/inventory/audit/recommend workflow, repository
files, and live GitHub CLI queries. No new framework or plugin was installed.
Codex desktop provides file access, shell tools, and native task coordination;
the repository's `.claude/settings.json` enables AI-mise for Claude, not Codex.
Reading and applying its skill here does not install it into Codex.

## State and action

| Area                | Evidence                                    | State / action                                                                     |
| ------------------- | ------------------------------------------- | ---------------------------------------------------------------------------------- |
| Checkout            | Git remote and HEAD                         | Checked out on `agent/C-Builder/harness-audit`, based on main                      |
| Agent entry point   | No tracked AGENTS.md at baseline            | Added a concise Codex entry point that routes to existing guidance                 |
| Local verification  | package.json, lint.yml                      | Aligned lint with CI's ESLint CLI; added explicit typecheck command                |
| Quality automation  | GitHub workflows API                        | Lint was `disabled_inactivity`; restored and verified `active`                     |
| Architecture        | src/payload.config.ts, collections, plugins | Corrected source paths and unsupported OAuth claims in CLAUDE.md                   |
| Review guidance     | .coderabbit.yaml, src tree                  | Replaced nonexistent layer rules with current Payload-oriented checks              |
| Historical guidance | HANDOFF.md                                  | Marked obsolete Sanity/Vercel instructions as historical, preserving the record    |
| Work tracking       | MIGRATION.md, asana-workflow.md             | Scoped Asana instructions to legacy reconciliation; GitHub is the new-work default |
| Local cache         | npm install                                 | Ignored the workspace npm cache in Git, formatting, and lint                       |

These are reversible branch changes. Re-enabling Lint is the only remote setting
changed. No deployment, production database operation, merge, or message to
Slack/Asana was performed.

## Visualization status

Both visualizations are generated snapshots on separate, unmerged branches. They
are not present on main, and have not been regenerated by this audit.

| Tool          | Result                                                                                                                                                                                                                                                                                                                     | Live PR state at audit                                                                                                                 |
| ------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| Graphify      | [Graph report](https://github.com/boulaajaj/sprecher-east-neighborhood-website/blob/agent/C-Builder/graphify-knowledge-graph/graphify-out/GRAPH_REPORT.md), [interactive HTML file](https://github.com/boulaajaj/sprecher-east-neighborhood-website/blob/agent/C-Builder/graphify-knowledge-graph/graphify-out/graph.html) | [PR #98](https://github.com/boulaajaj/sprecher-east-neighborhood-website/pull/98) open, CLEAN; reported checks pass                    |
| Code Cohesion | [Interactive standalone HTML file](https://github.com/boulaajaj/sprecher-east-neighborhood-website/blob/agent/C-Builder/codecohesion-3d-view/codecohesion-out/standalone.html)                                                                                                                                             | [PR #99](https://github.com/boulaajaj/sprecher-east-neighborhood-website/pull/99) open, UNSTABLE; build passes, CodeQL aggregate fails |

GitHub displays HTML source: download the HTML file and open it in a browser to
interact. Graphify requires its external vis-network library. Code Cohesion's
standalone file embeds its viewer and data. Copies were also extracted into this
Codex task's visualization folder for local viewing.

The PR descriptions report 1,640 Graphify nodes / 3,437 edges and 335 Code Cohesion
snapshot files / a 295-commit timeline. Those are prior generation results, not a
new audit measurement. Prior local memory reports 12 CodeQL alerts in the
vendored viewer; current branch/PR alert API queries returned no rows, so this
audit confirms the failed check but does not independently confirm that count or
dismiss any findings. Lint was absent from both PR check lists before restoration;
restoring the workflow does not retroactively prove those checks pass.

## Depth, assumptions, and remaining work

Verification: `npm ci --ignore-scripts` installed the locked dependencies;
`npm run prepare` then activated the existing Husky hook (`.husky/_`).
`npm run typecheck` passed. `npm run lint` passed with six existing warnings and
zero errors. Prettier passed on all changed Markdown, JSON, YAML, and JavaScript
files. `git diff --check` passed. A production build was not run: this change does
not alter application behavior, and no local database or runtime secrets were
provisioned.

The production-only npm audit reports 31 vulnerable packages: 12 moderate,
15 high, and 4 critical. These are package-level audit findings, not 31 proven
exploits. They include Payload-related packages and Next.js. Dependency upgrades
are deferred to security PRs with runtime/build verification; no versions were
changed in this harness cleanup. The restored dependency gate may therefore fail
until those updates land.

Understood sufficiently to change: agent entry routing, checked-in tool commands,
current configuration paths, obsolete review boundaries, and workflow activation.
Inspected only: specialist role profiles, application behavior, deployment
configuration, and prior visualization generation. This is not a full application
security review or production smoke test.

Remaining drift: specialist profiles still contain OAuth, `src/lib/data.ts`, and
other historical assumptions. AGENTS.md explicitly requires checking them against
the implementation. Migration completion and the GitHub Project board remain
unverified; no Asana data was archived. Local Node is 24.13.0 whereas CI specifies
Node 20, so local checks do not reproduce CI's runtime exactly.

Recommendation: keep the existing lightweight harness, use the new entry point,
and resolve Code Cohesion's failed security check before considering its merge.
Reconcile specialist profiles in a focused follow-up, using the current source
tree rather than propagating their older architecture descriptions.
