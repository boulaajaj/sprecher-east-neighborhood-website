npx lint-staged

# Block commits if high/critical vulnerabilities exist in new or changed deps
npm audit --audit-level=high --omit=dev 2>/dev/null || echo "⚠ npm audit found high/critical vulnerabilities — review before pushing"
